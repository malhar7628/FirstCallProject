enum ResponseStatus {
  success,
  failure,
  error,
}

enum Role { mu }
enum Status { active }

enum ChargingPointStatus {
  open,
  close,
}
enum ScheduleBookingStatus{
  Y,
  N,
}


const amounts = ["100", "500", "1000", "0"];

enum TransactionStatus { success, cancelled, failure, other }
